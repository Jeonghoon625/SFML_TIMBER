#pragma once

enum class Side
{
    LEFT,
    RIGHT,
    NONE,
};

enum class TextureName
{
    textureCloud,
    textureBackground,
    textureLog,
    textureAxe,
    textureRIP,
    textureBee,
    textureTree,
    textureBranch,
    textureCharacterback,
    texture1P,
    texture2P,
    textureList,
};

enum class FontName
{
    fontKOMIKAP,
};

enum class SoundBufferName
{
    chopBuffer,
    deathBuffer,
    ootBuffer,
};